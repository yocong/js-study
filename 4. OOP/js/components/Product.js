
class Product {
  constructor(title, price, desc, image) {
    this.title = title;
    this.price = price;
    this.desc = desc;
    this.image = image;
  }
}

// export default : 한 모듈에 하나, {} 생략가능, 이름 변경도 가능
export default Product;